syntax = "proto3";
package protocol;

message UntypedSingleValue {
    double Value = 1;
}

message MetricEvent {  
    uint64 Timestamp = 1;
    string Name = 2;  
    oneof Value {
        UntypedSingleValue UntypedSingleValue = 3;
    }
    map<string, bytes> Tags = 4;
}